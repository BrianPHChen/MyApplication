package com.example.myapplication.testutil

// Note: TestDatabase utility is not needed for unit tests
// as we use Mock DAO instead of real database.
// This file is kept for potential androidTest usage in the future.

object TestDatabase {
    
    // This would be used in androidTest, not unit test
    // Unit tests should use Mock DAO instead
    
}